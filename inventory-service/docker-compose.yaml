services:
  postgresql:
    image: postgres:15
    container_name: inventory
    environment:
      POSTGRES_PASSWORD: 2201
    ports:
      - "5434:5432"
    volumes:
      - ./postgres:/var/lib/postgres
services:
  postgresql:
    image: postgres:15
    container_name: user
    environment:
      POSTGRES_PASSWORD: 2201
    ports:
      - "5435:5432"
    volumes:
      - ./postgres:/var/lib/postgres
